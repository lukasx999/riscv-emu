#include <unistd.h>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

static int add(int a, int b) {
    return a + b;
}

int main(void) {

    putchar(65);
    // printf("");

    // __asm__ volatile ("li a0, 0");
    // __asm__ volatile ("li a7, 93");
    // __asm__ volatile ("ecall");

    return 0;

}
